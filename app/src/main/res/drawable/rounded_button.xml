<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <corners android:radius="400sp" />
    <stroke
        android:width="2sp"
        android:height="2sp"
        android:color="@color/secondary" />
    <solid android:color="@color/background" /> <!-- Set your desired background color here -->
</shape>
